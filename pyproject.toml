[project]
name = "convert"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "msgpack>=1.1.2",
    "protobuf>=7.34.1",
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0.3",
]
